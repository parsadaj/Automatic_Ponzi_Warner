import os


def get_urls_from_file(train_specifier):
    pass

def append_to_unigram_file(url, unigram_path):
    pass
    
def create_if_not_exist(directory):
    if not os.path.exists(directory):
        os.mkdir(directory)